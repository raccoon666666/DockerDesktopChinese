{
  "include": ["./src/**/*.json", "./src/**/*.ts"],
  "extends": "../tsconfig.web.json",
  "compilerOptions": {
    // this package builds with webpack, so this outDir is only used in the CI step that checks that all our TS builds successfully
    "outDir": "./__check-ts__",
    "rootDir": "./src",

    "resolveJsonModule": true
  },
  "references": [
    { "path": "../extension-api-client/extension-api-client-ipc" },
    { "path": "../desktop-theme" }
  ]
}
