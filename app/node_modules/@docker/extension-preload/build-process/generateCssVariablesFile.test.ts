import { generateCssVariableFileContents } from './generateCssVariablesFile';

describe('Extension CSS variables', () => {
  it('should match snapshot', () => {
    // guards against accidental changes to the CSS vars
    expect(generateCssVariableFileContents()).toMatchSnapshot();
  });
});
